sudo apt-get update

curl -fsSL https://deb.nodesource.com/setup_current.x | bash -
apt install -y nodejs 

sudo apt-get install -y git (install git optional)

git clone https://<your-username>:<your-token>@github.com/Animeshs786/Cosmic-Wedding.git (clone git project using token)

cd your-nodejs-project

npm install

node server.js (to run the project)

sudo npm install -g pm2 (install pm2 globally)

pm2 resurrect (to start all project)

pm2 start server.js --name cosmic-wedding

pm2 start npm --name "next-app" -- start (next pm2 start code )

pm2 start "npx next start -p 3007" --name AsteroKashiFrontend

pm2 startup

pm2 save

sudo ufw enable

sudo ufw allow 22 (for ssh)

sudo ufw allow 80 (for http)

sudo ufw allow 443 (for https)

sudo ufw allow 5001 (for local port value)

//permission command
sudo chown -R ubuntu:ubuntu /home/ubuntu/triptaktix
sudo chmod -R 775 /home/ubuntu/triptaktix

sudo mkdir /var/www/cosmic-wedding

sudo chmod -R 777 /var/www/cosmic-wedding

sudo apt-get install -y nginx

sudo nano /etc/nginx/sites-available/default (Open nginx file)

server {
    listen 80;
    server_name 54.253.250.87;  # Replace this with your server's IP

    root /var/www/cosmic-wedding;  # Set the root to your React build directory
    index index.html index.htm;

    location / {
        try_files $uri /index.html;  # Serve the React app for any request
    }

    location /api/ {
        proxy_pass http://localhost:5001;  # Proxy API requests to your Node.js app
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location /public/ {
        proxy_pass http://127.0.0.1:4005/public/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
}

# Configuration for the first project (cosmic-wedding)
server {
    listen 80;
    server_name 54.253.250.87;  # Replace this with your server's IP or domain

    root /var/www/cosmic-wedding;  # Set the root to your React build directory
    index index.html index.htm;

    location / {
        try_files $uri /index.html;  # Serve the React app for any request
    }

    location /api/ {
        proxy_pass http://localhost:5001;  # Proxy API requests to your Node.js app
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# Configuration for the second project
server {
    listen 80;
    server_name second.54.253.250.87;  # Replace this with your server's IP or domain for the second project

    root /var/www/second-project;  # Set the root to your second project's build directory
    index index.html index.htm;

    location / {
        try_files $uri /index.html;  # Serve the second project's frontend for any request
    }

    location /second-api/ {
        proxy_pass http://localhost:8000;  # Proxy API requests to the second Node.js app
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# Admin Panel Server Block
server {
    listen 80;
    server_name admin.triptaktix.com;

    root /var/www/tripTak;  # Set the root to your React build directory
    index index.html index.htm;

    location / {
        try_files $uri /index.html;  # Serve the React app for any request
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8000;  # Proxy API requests to your Node.js app
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        client_max_body_size 100M;
    }
}

# Frontend and API Server Block
server {
    listen 80;
    server_name triptaktix.com www.triptaktix.com;

    # Frontend
    location / {
        proxy_pass http://127.0.0.1:3000;  # Frontend app
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # API
    location /api/ {
        proxy_pass http://127.0.0.1:8000;  # Proxy API requests to your Node.js app
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
     location /public/ {
        proxy_pass http://127.0.0.1:4044/public/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

}

server {
         listen 85 default_server;
        listen [::]:85 default_server;
        root /var/www/html/pmj;

        index index.html index.htm index.nginx-debian.html;

        server_name _;

        location / {
                try_files $uri /index.html;
        }

}


sudo nginx -t (after edit to check status)

sudo systemctl restart nginx

sudo nano /etc/nginx/sites-available/default (to open file)

// ssl certificate code

sudo apt update

sudo apt install certbot python3-certbot-nginx

sudo certbot --nginx -d admin.cosmicwedding.in

sudo certbot renew --dry-run

//find any folder in server
sudo find / -type d 2>/dev/null | grep -i "pmj"

//slaepare
nvm install 14
nvm use 14
nvm use 14  # Or the Node.js version your project uses
which pm2
npm install -g pm2

//timezone on server
sudo timedatectl set-timezone Asia/Kolkata

//puppteer (pdf making) 
sudo apt-get update
sudo apt-get install -y libpangocairo-1.0-0 libx11-xcb1 libxcomposite1 libxcursor1 libxdamage1 libxi6 libxtst6 libnss3 libcups2t64 libxss1 libxrandr2 libatk1.0-0t64 libgtk-3-0t64 libasound2t64 libgbm-dev

sudo systemctl restart packagekit.service
sudo systemctl restart systemd-resolved.service

systemctl status packagekit.service
systemctl status systemd-resolved.service


